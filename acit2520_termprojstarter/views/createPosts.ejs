<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.classless.min.css"
    />
    <link href="/css/style.css" rel="stylesheet" />
    <title>Create Post</title>
  </head>
  <body>
    <main>
      <div class="authOptions">
        <h1>Create Post</h1>
        <a role="button" href="/posts">Home</a>
      </div>
  
      <!-- Error Message Display -->
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert" style="color: red; padding: 0.5rem; margin-bottom: 1rem;">
          <%= error %>
        </div>
      <% } %>
  
      <!-- Create Post Form -->
      <form action="/posts/create" method="POST">
        <!-- Title Field -->
        <label for="title">
          Title*
          <input 
            type="text" 
            id="title" 
            name="title" 
            required
            value="<%= formData.title %>"
            placeholder="Enter post title">
        </label>
  
        <!-- Subgroup Field -->
        <label for="subgroup">
          Community (Subgroup)*
          <input 
            type="text" 
            id="subgroup" 
            name="subgroup" 
            required
            value="<%= formData.subgroup %>"
            placeholder="e.g. 'food', 'coding'">
        </label>
  
        <!-- Link Field -->
        <label for="link">
          Link (optional)
          <input 
            type="url" 
            id="link" 
            name="link"
            value="<%= formData.link %>"
            placeholder="https://example.com">
        </label>
  
        <!-- Description Field -->
        <label for="description">
          Description <% if (!formData.link) { %>(required)<% } else { %>(optional)<% } %>
          <textarea 
            id="description" 
            name="description"
            rows="4"
            placeholder="Share your thoughts..."><%= formData.description %></textarea>
        </label>
  
        <button type="submit" style="margin-top: 1rem;">
          Create Post
        </button>
      </form>
    </main>
  </body>
  </html>
